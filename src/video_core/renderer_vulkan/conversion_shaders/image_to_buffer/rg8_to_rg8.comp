#version 450
#include "../utils.comp"

layout(binding = 0) writeonly buffer dst_buffer {
    u8vec2 colors[];
};

layout(binding = 1, rg8ui) uniform readonly lowp uimage2D src_image;

void main() {
    u8vec2 rg8 = u8vec2(imageLoad(src_image, GetImageCoord()).rg);
    colors[GetAddress()] = rg8;
}
